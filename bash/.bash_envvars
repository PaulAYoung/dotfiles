#!/usr/bin/env bash
# set environment variables

export PATH=~/npm/bin:$PATH

export ANDROID_HOME=~/Android/Sdk

#add user's bin path if the directory exists
if [ -d ~/bin ]; then
    export PATH=~/bin:$PATH
fi
